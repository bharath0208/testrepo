service {
  host = "https://localhost:18803"
  token = ${?WML_CPD_SERVICE_CRED}
}

cluster {
  url = "https://api.dataplatform.dev.cloud.ibm.com"
}
client {
  auth = "IAM"
}

icp {
  token {
    username = "admin"
    password = "password"
    url = "https://dev-53-cpd-dev-53.apps.frank-v4-repo.os.fyre.ibm.com"
  }
}

fvt {
  iam {
    apikey = ${WML_API_KEY}
    apikey_beta = ${WML_API_KEY_V4BETA}
    // required to look up the IAM token
    token_server = "https://iam.test.cloud.ibm.com/identity/token"
    authorization = "Basic Yng6Yng="
    cache-token = true
    username = ${WML_TEST_USERNAME}
    password = ${WML_TEST_PASSWORD}
    uaa_client_id = "cf"
    uaa_client_secret = ""
    bss_account_id = ${WML_TEST_ACCOUNT_ID}
  }
  cos {
    resource_crn = ${WML_COS_CRN}
  }
  wml {
    instance_crn = ${WML_INSTANCE_CRN}
  }
}
